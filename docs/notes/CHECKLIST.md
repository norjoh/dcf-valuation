make model that can evalueate suggested inputs
male model that can find different names of "same" fields in all the yahoo data
in auto_metrics rows with nan are dropped, so include some counter over what is dropped and take that into account in the evaluation of the numbers
make model that cah look trough recent quarterly report and find outlooks, and if not do estimates based on numbers found in report
create bank specific valuation model
write about using yfinance as a risk
- why is so much data missing?